int main(int argc, char **argv) {
    for (int i = 0; i < argc; ++i) {
        printf("argv[%d]: %s\n", i, argv[i]);
    }

    // Operators:
    -> <- += -= >= <= == != ( ) [ ] { } | || && & , . / * + - %

    printf("Hello World!\n");

    int i = 10;
    double d = 1.5;
    string s = "String";

    printf("%d\n%f\n%s", i, d, s);
}
